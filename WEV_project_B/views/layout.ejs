<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | FitMeal</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/about.css">
    <link rel="stylesheet" href="/css/contact.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">FitMeal</a>
            <button class="nav-toggle" aria-label="תפריט">
                <i class="fas fa-bars"></i>
            </button>
            <div class="nav-links">
                <a href="/" class="nav-link <%= path === '/' ? 'active' : '' %>">דף הבית</a>
                <a href="/about" class="nav-link <%= path === '/about' ? 'active' : '' %>">אודות</a>
                <a href="/contact" class="nav-link <%= path === '/contact' ? 'active' : '' %>">צור קשר</a>
                <% if (locals.user) { %>
                    <a href="/planner" class="nav-link <%= path === '/planner' ? 'active' : '' %>">תכנון ארוחות</a>
                    <a href="/auth/logout" class="nav-link">התנתק</a>
                <% } else { %>
                    <a href="/auth/login" class="nav-link <%= path === '/auth/login' ? 'active' : '' %>">התחבר</a>
                    <a href="/auth/signup" class="nav-link <%= path === '/auth/signup' ? 'active' : '' %>">הרשמה</a>
                <% } %>
            </div>
        </div>
    </nav>

    <main>
        <%- body %>
    </main>

    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>אודות</h3>
                <p>FitMeal - מערכת לתכנון ארוחות בריאות</p>
            </div>
            <div class="footer-section">
                <h3>קישורים מהירים</h3>
                <ul>
                    <li><a href="/">דף הבית</a></li>
                    <li><a href="/planner">מתכנן ארוחות</a></li>
                    <li><a href="/contact">צור קשר</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; <%= new Date().getFullYear() %> FitMeal. כל הזכויות שמורות.</p>
        </div>
    </footer>

    <script src="/js/main.js"></script>
    <% if (locals.title === 'צור קשר') { %>
        <script src="/js/contact.js"></script>
    <% } %>
    <script>
        document.querySelector('.nav-toggle').addEventListener('click', function() {
            document.querySelector('.nav-links').classList.toggle('active');
        });
    </script>
</body>
</html> 